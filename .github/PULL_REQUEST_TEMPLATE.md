## Change

Describe the problem and resulting behavior. Link a related issue if applicable.

## Validation

Describe the checks run and their results. Distinguish deterministic tests from live-model evaluation.

- [ ] Relevant checks pass (`npm run check` and `npm test` for code changes).
- [ ] User-facing documentation and the changelog are updated where needed.
- [ ] The diff contains no credentials, private project details, local session data, or workstation paths.
